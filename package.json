{
  "name": "api-extractor-remap",
  "version": "1.0.0",
  "description": "Remap api-extractor markdown",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest --passWithNoTests",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bengreenier-actions/api-extractor-remap.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "Ben Greenier",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.5",
    "@actions/glob": "^0.2.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "eslint": "^8.14.0",
    "jest": "^27.2.5"
  },
  "engines": {
    "node": ">=16"
  }
}
